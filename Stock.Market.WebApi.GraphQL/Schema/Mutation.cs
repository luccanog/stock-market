namespace Stock.Market.WebApi.GraphQL.Schema
{
    public class Mutation
    {
        public bool BuyStockShares(string symbol)
        {
            // Send request to https://api.nasdaq.com/api/quote/AAPL/info?assetclass=stocks and check if the SYMBOL exists

            // Send a KAFKA message

            return true;
        }
    }
}
